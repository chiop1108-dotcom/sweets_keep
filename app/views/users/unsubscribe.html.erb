<h2>本当に退会しますか？</h2>
<p>退会すると、これまでの投稿データなどはすべて削除されます。</p>

<div>
  <!-- 「はい」を押した場合は削除処理（DELETEリクエスト）を実行 -->
  <%= link_to "はい（退会する）", user_path(@user), method: :delete %>

  <!-- 「いいえ」を押した場合は詳細画面に戻る -->
  <%= link_to "いいえ（マイページへ戻る）", user_path(@user) %>
</div>